# Prime Number Finder

This program includes two functions: `prime_number(num)` and `nth_prime_number(n)`. The `prime_number` function checks if a given number is prime, and the `nth_prime_number` function finds the nth prime number.